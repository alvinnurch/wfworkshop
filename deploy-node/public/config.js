/* Versi server sendiri: aplikasi dan API berada di alamat yang sama,
   jadi tidak ada kunci atau URL yang perlu diisi.
   Sandi admin diperiksa server (variabel ADMIN_PASSWORD), bukan di halaman ini. */
window.WORKSHOP_API = '/api';
